<div id="artists">
  <ul>
    <% @directories.each do |directory| %>
      <li><a href="<%= make_path(directory.split('/').last) %>"><%= directory.split('/').last %></a></li>
    <% end %>
  </ul>
</div>
